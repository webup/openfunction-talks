{
  "private": true,
  "scripts": {
    "build": "rimraf dist && pnpm -r run build"
  },
  "devDependencies": {
    "@iconify/json": "^2.1.38",
    "@slidev/cli": "^0.31.2",
    "@slidev/theme-default": "^0.21.2",
    "pnpm": "^7.0.0",
    "rimraf": "^3.0.2"
  }
}
